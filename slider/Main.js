window.onload = function() {
    let slide = new Slider();
    slide.afficherImage();//affiche la première image
    slide.modifierImageAv(); //modifie l'image au clic
    slide.modifierImageArr(); //modifie l'image au clic
}

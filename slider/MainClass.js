window.onload = function() {
    let slider = new SliderClass();
    slider.displayFirstImg(); //affiche première image
    slider.moveForward(); //lecture en avant
    slider.moveBackward(); //lecture en arrière
}